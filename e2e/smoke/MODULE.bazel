bazel_dep(name = "com_booking_rules_patchelf_prebuilt", version = "0.0.0", dev_dependency = True)
bazel_dep(name = "bazel_skylib", version = "1.6.1", dev_dependency = True)

local_path_override(
    module_name = "com_booking_rules_patchelf_prebuilt",
    path = "../..",
)
